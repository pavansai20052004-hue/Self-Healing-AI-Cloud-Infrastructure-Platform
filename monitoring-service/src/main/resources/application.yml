server:
  address: 0.0.0.0
  port: ${PORT:8081}

spring:
  application:
    name: monitoring-service

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      probes:
        enabled: true
